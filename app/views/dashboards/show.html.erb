<div>
  <h2> <%= current_user.first_name %> </h2>
  <%= current_user.last_name %>
  <%= current_user.email %>
  <p>Hello</p>

   <%= cl_image_tag @user.photo.key, wdth: 200, height: 400, crop: :fill %>

</div>
